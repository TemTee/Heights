# Load the data and libraries -----------------------------------------------------

library(tidyverse)
library(dplyr)
library(dslabs)
data(heights)
options(digits = 3)

# Data Exploration ----------------------------------------------------------------
str(heights)
